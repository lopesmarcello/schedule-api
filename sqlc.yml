
version: 2

sql:
  - engine: "postgresql"
    queries: "./internal/repositories/pg/queries"
    schema: "./internal/repositories/pg/migrations"
    gen:
      go:
        emit_json_tags: true
        out: "./internal/repositories/pg"
        package: "pg"
        sql_package: "pgx/v5"
